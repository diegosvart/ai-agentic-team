version: "3.9"
services:
  agentic-ti-os:
    build:
      context: ../..
      dockerfile: runtime/docker/Dockerfile
    env_file: .env
    volumes:
      - ../../src:/app/src
    stdin_open: true
    tty: true
